@model HotelReservationSystem.Models.ReservationRoomModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>ReservationRoomModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="Reservation_Id" class="control-label"></label>
                <select asp-for="Reservation_Id" class="form-control" asp-items="ViewBag.Reservations"></select>
                <span asp-validation-for="Reservation_Id" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Room_Id" class="control-label"></label>
                <select asp-for="Room_Id" class="form-control" asp-items="ViewBag.Rooms"></select>
                <span asp-validation-for="Room_Id" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
